 "use strict";
 let planets = [
     'Mercury',
     'Venus',
     'Earth',
     'Mars',
     'Jupiter',
     'Saturn',
     'Uranus',
     'Neptune',
 ];
 planets.push(response);
 for (var i = 0; i < planets.length; i++){
        planets.push(planets[i]);
    } console.log(planets);

 planets.push(response);
    for (var i = 7; i < planets.length; i++){
        planets.push(planets[i]);
    } console.log(planets);

 planets.push(response);
    alert(planets.shift());
planets.push(response);
    alert(planets.pop());

console.log(planets[2]);

   function planetsReverse(input){
       console.log(input.reverse().join("\n");)}
   }