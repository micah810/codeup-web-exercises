function isOdd(number){
    if(!isNaN(number) )
}

function isEven(a){
    if(!isNaN(a) )
}

function identity(input){

}

function isFive(f){

}

function addFive(m, n){
    if(!isNaN(m) && !isNaN(n))
        else(m = 5);
    if else( m + n);
}

function isMultipleOfFive()

function isTwo(a){
    if(!isNaN(a)
    functionjavascriptffvfvdttping for noise)
}